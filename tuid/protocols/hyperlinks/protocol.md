# Protocol: OSC Hyperlinks

Terminal-clickable hyperlinks (OSC 8).

- Enhancement only (OPTIONAL) — the same target must be reachable via keyboard (copy path, open action).
- Never the sole affordance for a required action.
